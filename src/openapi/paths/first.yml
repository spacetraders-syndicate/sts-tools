get:
  operationId: first
  summary: first endpoint
  tags:
   - first
  responses:
   200:
    description: Success
    content:
     application/json:
      schema:
       $ref: ../schemas/first.yml